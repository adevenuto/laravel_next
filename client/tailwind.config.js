import animate from 'tailwindcss-animate'

/** @type {import('tailwindcss').Config} */
export default {
  darkMode: ['class'],
  content: ['./index.html', './src/**/*.{vue,js,ts,jsx,tsx}'],
  theme: {
    container: {
      center: true,
      padding: '2rem',
      screens: {
        '2xl': '1400px',
      },
    },
    extend: {
      colors: {
        border: 'hsl(var(--border))',
        input: 'hsl(var(--input))',
        ring: 'hsl(var(--ring))',
        background: 'hsl(var(--background))',
        foreground: 'hsl(var(--foreground))',
        primary: {
          DEFAULT: 'hsl(var(--primary))',
          foreground: 'hsl(var(--primary-foreground))',
        },
        secondary: {
          DEFAULT: 'hsl(var(--secondary))',
          foreground: 'hsl(var(--secondary-foreground))',
        },
        destructive: {
          DEFAULT: 'hsl(var(--destructive))',
          foreground: 'hsl(var(--destructive-foreground))',
        },
        muted: {
          DEFAULT: 'hsl(var(--muted))',
          foreground: 'hsl(var(--muted-foreground))',
        },
        accent: {
          DEFAULT: 'hsl(var(--accent))',
          foreground: 'hsl(var(--accent-foreground))',
        },
        popover: {
          DEFAULT: 'hsl(var(--popover))',
          foreground: 'hsl(var(--popover-foreground))',
        },
        card: {
          DEFAULT: 'hsl(var(--card))',
          foreground: 'hsl(var(--card-foreground))',
        },
        // Project-specific state colors — distinct from --destructive on purpose.
        coach: {
          DEFAULT: 'hsl(var(--coach))',
          foreground: 'hsl(var(--coach-foreground))',
        },
        success: {
          DEFAULT: 'hsl(var(--success))',
          foreground: 'hsl(var(--success-foreground))',
        },
      },
      fontFamily: {
        display: ['var(--font-display)', 'serif'],
        reading: ['var(--font-reading)', 'sans-serif'],
        sans: ['var(--font-reading)', 'sans-serif'],
        mono: ['var(--font-mono)', 'monospace'],
      },
      borderRadius: {
        lg: 'var(--radius)',
        md: 'calc(var(--radius) - 2px)',
        sm: 'calc(var(--radius) - 4px)',
        kid: 'var(--radius-kid)',
        soft: 'var(--radius-soft)',
        pill: 'var(--radius-pill)',
      },
      boxShadow: {
        raised: 'var(--shadow-raised)',
        lifted: 'var(--shadow-lifted)',
      },
      transitionDuration: {
        quick: 'var(--motion-duration-quick)',
        lush: 'var(--motion-duration-lush)',
      },
      transitionTimingFunction: {
        quick: 'var(--motion-ease-quick)',
        lush: 'var(--motion-ease-lush)',
      },
      keyframes: {
        'accordion-down': {
          from: { height: '0' },
          to: { height: 'var(--reka-accordion-content-height)' },
        },
        'accordion-up': {
          from: { height: 'var(--reka-accordion-content-height)' },
          to: { height: '0' },
        },
        // Answer-correct pop. Used by exercise base primitives. Quick.
        'correct-pop': {
          '0%': { transform: 'scale(1)' },
          '40%': { transform: 'scale(1.06)' },
          '100%': { transform: 'scale(1)' },
        },
        // Wrong-answer nudge. Horizontal, gentle. NEVER a shake.
        'coach-nudge': {
          '0%, 100%': { transform: 'translateX(0)' },
          '25%': { transform: 'translateX(-4px)' },
          '75%': { transform: 'translateX(4px)' },
        },
      },
      animation: {
        'accordion-down': 'accordion-down 0.2s ease-out',
        'accordion-up': 'accordion-up 0.2s ease-out',
        'correct-pop': 'correct-pop var(--motion-duration-quick) var(--motion-ease-quick)',
        'coach-nudge': 'coach-nudge var(--motion-duration-quick) var(--motion-ease-quick)',
      },
    },
  },
  plugins: [animate],
}
