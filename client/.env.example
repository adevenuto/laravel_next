# Leave VITE_API_URL empty in development to use the Vite dev proxy
# (forwards /api and /sanctum to http://localhost:8000 — keeps the browser same-origin)
# In production CI, set this via the GH secret VITE_API_URL to the public Laravel API host.
VITE_API_URL=
